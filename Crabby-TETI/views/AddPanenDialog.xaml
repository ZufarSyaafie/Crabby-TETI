<Window x:Class="CrabbyTETI.views.AddPanenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input Data Panen" 
        Height="550" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF6B35"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF6B35"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.3" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FF6B35" CornerRadius="15,15,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#x1F4E6;" FontSize="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Input Data Panen" 
                                   FontSize="20" 
                                   FontWeight="Bold" 
                                   Foreground="White" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Button Content="?" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"
                            Width="30" 
                            Height="30"
                            FontSize="16" 
                            FontWeight="Bold"
                            Background="Transparent" 
                            Foreground="White" 
                            BorderThickness="0" 
                            Cursor="Hand"
                            Click="BtnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E55A2B"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Form Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30,20">
                <StackPanel>
                    <!-- Pilih Tambak -->
                    <TextBlock Text="Pilih Tambak" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                    <ComboBox x:Name="cmbTambak" 
                              SelectedValuePath="Id"
                              FontSize="14"
                              Padding="10,8"
                              Margin="0,0,0,15">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Nama}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Lokasi}" FontSize="11" Foreground="#999999"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Tanggal Panen -->
                    <TextBlock Text="Tanggal Panen" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                    <DatePicker x:Name="dpTanggalPanen" 
                                FontSize="14"
                                Padding="10,8"
                                Margin="0,0,0,15"/>

                    <!-- Jumlah (kg) -->
                    <TextBlock Text="Jumlah Panen (kg)" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtJumlahKg" 
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,15"/>

                    <!-- Harga per Kg (Opsional) -->
                    <TextBlock Text="Harga per Kg (Opsional)" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtHargaPerKg" 
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,5"/>
                    <TextBlock Text="Kosongkan jika belum ada harga" 
                               FontSize="11" 
                               Foreground="#999999" 
                               Margin="0,0,0,15"/>

                    <!-- Total Nilai (Auto-calculated) -->
                    <Border Background="#F5F7FA" 
                            CornerRadius="8" 
                            Padding="15" 
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Total Nilai Panen" 
                                       FontSize="13" 
                                       Foreground="#666666" 
                                       Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtTotalNilai" 
                                       Text="Rp 0" 
                                       FontSize="20" 
                                       FontWeight="Bold" 
                                       Foreground="#FF6B35"/>
                        </StackPanel>
                    </Border>

                    <!-- Keterangan (Opsional) -->
                    <TextBlock Text="Keterangan (Opsional)" FontSize="13" Foreground="#666666" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtKeterangan" 
                             Style="{StaticResource ModernTextBox}"
                             Height="80"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,15"/>

                    <!-- Status Message -->
                    <TextBlock x:Name="txtStatusMessage"
                               FontSize="12"
                               Foreground="Red"
                               TextWrapping="Wrap"
                               Margin="0,10,0,0"
                               Visibility="Collapsed"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Padding="20,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" 
                            Content="Batal" 
                            Margin="0,0,10,0"
                            Padding="0,12"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Background="#E0E0E0"
                            Foreground="#666666"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="BtnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#CCCCCC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Grid.Column="1" 
                            Content="&#x1F4BE; Simpan Data Panen" 
                            Margin="10,0,0,0"
                            Padding="0,12"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="BtnSave_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45A049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
